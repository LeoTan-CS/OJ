export const dynamic = "force-dynamic";
import { AdminNav, AppShell } from "@/components/shell";
import { Card } from "@/components/ui";
import { DeleteButton, JsonForm } from "@/components/admin-forms";
import { prisma } from "@/lib/prisma";

export default async function UsersPage() { const [users, classes] = await Promise.all([prisma.user.findMany({ include: { class: true }, orderBy: { createdAt: "desc" } }), prisma.class.findMany()]); const classOptions=[{label:'无班级',value:''},...classes.map(c=>({label:c.name,value:c.id}))]; return <AppShell admin><AdminNav /><div className="grid gap-6 lg:grid-cols-[380px_1fr]"><Card><h1 className="text-xl font-bold">新建用户</h1><div className="mt-4"><JsonForm endpoint="/api/admin/users" fields={[{name:'username',label:'用户名'},{name:'password',label:'初始密码',type:'password'},{name:'nickname',label:'昵称'},{name:'role',label:'角色',options:[{label:'USER',value:'USER'},{label:'ADMIN',value:'ADMIN'},{label:'SUPER_ADMIN',value:'SUPER_ADMIN'}]},{name:'classId',label:'班级',options:classOptions},{name:'enabled',label:'启用',type:'checkbox'}]} /></div></Card><Card><h1 className="text-xl font-bold">用户列表</h1><table className="mt-3"><tbody>{users.map((u)=><tr key={u.id}><td>{u.nickname}<p className="text-xs text-slate-500">{u.username}</p></td><td>{u.role}</td><td>{u.class?.name??'-'}</td><td>{u.enabled?'启用':'禁用'}</td><td><DeleteButton endpoint={`/api/admin/users/${u.id}`} /></td></tr>)}</tbody></table></Card></div></AppShell>; }
